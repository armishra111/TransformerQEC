# Notebooks

The original notebooks are historical artifacts. Once package parity is in place, move them under `notebooks/archive/` and treat the package CLI plus docs as the canonical workflow.

Use the notebooks for provenance and exploratory context only. New baseline and research work should be captured through checked-in configs, package commands, saved artifacts, and the docs contracts in `docs/`.
